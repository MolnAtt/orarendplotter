\definecolor{te}{HTML}{FACEBC}  % Pompadour(rózsaszín)
\definecolor{gyte}{HTML}{FACEBC}  % Pompadour(rózsaszín)
\definecolor{mt}{HTML}{B09CD9}  % ringlo(lila)
\definecolor{msz}{HTML}{CCAEF1}  %  akác(lila)
\definecolor{mts}{HTML}{E3A6F8}  % orgona(lila)
\definecolor{of}{HTML}{C70081}  % BAZSARÓZSAVÖRÖS (bíbor)
\definecolor{r}{HTML}{FF1900}  % skarlát(VÖRÖS)
\definecolor{mte}{HTML}{E80606}  % ribizli(VÖRÖS)
\definecolor{{\' e}n}{HTML}{FBCB13}  % kukorica(sárga)
\definecolor{{\' e}k}{HTML}{F5E748}  % kanári(sárga) 
\definecolor{zt}{HTML}{F4CE69}  % barokk(sárga)
\definecolor{et}{HTML}{EEEAE2}  % Ekrü
\definecolor{i}{HTML}{F0C76A} % MUSKOTÁLY(sárga)
\definecolor{ny}{HTML}{F5C533} % Monarchia(sárga)
\definecolor{a}{HTML}{FDF74F} % HANZA(sárga)
\definecolor{asz}{HTML}{FFF566} % kén(sárga)
\definecolor{ol}{HTML}{7EC476} % eozin(zöld)
\definecolor{la}{HTML}{CDF3AA} % alma(zöld)
\definecolor{inf}{HTML}{31BDFF}  % lézer(kék)
\definecolor{ao}{HTML}{FFEC80}  % Mimóza
\definecolor{t{\" o}}{HTML}{CC483F}  % Sárkányvér(vörös)
\definecolor{{\' e}v}{HTML}{B04A4A}  % Velencei(vörös)
\definecolor{civ}{HTML}{73A500}  % KLOROFIL(zöld)
\definecolor{n{\' e}}{HTML}{FA493C}  % korall(piros)
\definecolor{mt{\" o}}{HTML}{719A0E}    % kadmium(zöld)
\definecolor{f{\" o}}{HTML}{5FFF30}  % papgáj(zöld)
\definecolor{k{\' e}}{HTML}{EA71EA}  % ciklámen(lila)
\definecolor{or}{HTML}{EEEAE2}  % selyem(fehér)
\definecolor{fi}{HTML}{C68A6A}  % vörhenyes(barna)
\definecolor{fr}{HTML}{99BCCC}  % acél(kék)
\definecolor{bi}{HTML}{F96960}  % eper(piros)
\definecolor{tm}{HTML}{EEC532}  % Õszarany
\definecolor{sp}{HTML}{FFA20C}  % sáfrány(sárga)
\definecolor{m{\' e}}{HTML}{A5B5F7}  % katángkóró(kék)
\definecolor{{\' a}g}{HTML}{C37ED2}  % perkin(lila)
\definecolor{fil}{HTML}{EDE2CA}  % GYAPJÚ(fehér)